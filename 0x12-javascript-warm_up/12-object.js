#!/usr/bin/node

// Create an object with properties 'type' and 'value'
const obj = {
  type: 'object',
  value: 12
};

// Log the state of the object to the console
console.log(obj);
obj.value = 89;
console.log(obj);
